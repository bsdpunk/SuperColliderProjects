s.boot;
s.scope;
FreqScope.new;
~three = Quarks.load("threed")

{
	var boom;
var w, width = 500, height = 400, rate = 0.005;

	w = Window("3d canvas demo", Rect(width, height), false).front;
	Class.methods.postln
	//boom = Ca
	// Canvas3D(w, Rect(width, height))
	// .scale_(200)
	// .perspective_(0.3)
	// .distance_(2)
	// .add(
	// 	Canvas3DItem.cube
	// 	.transform(Canvas3D.mRotateY(pi/3))
	// 	.transform(Canvas3D.mRotateX(pi/8))
	// );
}



